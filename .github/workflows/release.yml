name: Release plugin

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  validate:
    name: Validate before release
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Validate plugin
        run: |
          python -m json.tool manifest.json >/dev/null
          python -m unittest discover -s tests -v
          bash -n scripts/install.sh

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Test Go backend
        run: go test -race ./...

  build-binaries:
    name: Build ${{ matrix.goos }}/${{ matrix.goarch }} binary
    needs: validate
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Build binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          version="${GITHUB_REF_NAME#v}"
          out="feader-rss-fetch_${version}_${GOOS}_${GOARCH}"
          checksum="checksums_${GOOS}_${GOARCH}.txt"
          go build -trimpath -ldflags "-s -w -X main.version=${version}" -o "${out}" ./cmd/feader-rss-fetch
          sha256sum "${out}" > "${checksum}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: binary-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            feader-rss-fetch_*
            checksums_${{ matrix.goos }}_${{ matrix.goarch }}.txt
          retention-days: 1

  release:
    name: Package and publish release
    needs: build-binaries
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Download all binaries
        uses: actions/download-artifact@v4
        with:
          pattern: binary-*
          path: dist
          merge-multiple: true

      - name: Combine checksums
        run: |
          shopt -s nullglob
          checksum_files=(dist/checksums_*.txt)
          if [ "${#checksum_files[@]}" -ne 4 ]; then
            echo "Expected four per-platform checksum files, found ${#checksum_files[@]}" >&2
            exit 1
          fi
          sort -k2 "${checksum_files[@]}" > checksums.txt
          if [ "$(wc -l < checksums.txt)" -ne 4 ]; then
            echo "Expected four checksum entries" >&2
            exit 1
          fi
          rm -f "${checksum_files[@]}"

      - name: Read plugin metadata
        id: metadata
        run: |
          python3 - <<'PY' >> "$GITHUB_OUTPUT"
          import json
          from pathlib import Path

          manifest = json.loads(Path("manifest.json").read_text())
          print(f"plugin_id={manifest['id']}")
          print(f"plugin_name={manifest['name']}")
          PY

      - name: Build plugin archive
        env:
          PLUGIN_ID: ${{ steps.metadata.outputs.plugin_id }}
          PLUGIN_VERSION: ${{ github.ref_name }}
        id: package
        run: |
          archive="${PLUGIN_ID}-${PLUGIN_VERSION}.zip"
          zip -q "$archive" \
            manifest.json \
            BarWidget.qml \
            Panel.qml \
            README.md \
            LICENSE \
            preview.png \
            example-config.json
          echo "archive=$archive" >> "$GITHUB_OUTPUT"

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v1
        with:
          subject-path: |
            dist/feader-rss-fetch_*
            checksums.txt
            ${{ steps.package.outputs.archive }}

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            ${{ steps.package.outputs.archive }}
            dist/feader-rss-fetch_*
            checksums.txt
